---
interface Props {
	schema: Record<string, unknown>;
}

const { schema } = Astro.props as Props;
---

<script type="application/ld+json" is:inline set:html={JSON.stringify({ '@context': 'https://schema.org', ...schema })} />
